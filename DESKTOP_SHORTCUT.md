# デスクトップショートカットの作り方

このガイドでは、デスクトップアイコンをダブルクリックするだけでアプリを起動できるようにする方法を説明します。

## 方法1: VBScriptショートカット（おすすめ - 静かに起動）

### 1. ショートカットを作成

1. デスクトップで右クリック → 「新規作成」→「ショートカット」
2. 「項目の場所を入力してください」に以下を入力：
   ```
   C:\Users\okamo\OneDrive\デスクトップ\google-calender-get-void\start-app-silent.vbs
   ```
3. 「次へ」をクリック
4. 名前を入力：`Google Calendar 空き時間`
5. 「完了」をクリック

### 2. アイコンを変更（オプション）

1. 作成したショートカットを右クリック → 「プロパティ」
2. 「アイコンの変更」をクリック
3. 好きなアイコンを選択（カレンダーアイコンなど）
4. 「OK」→「適用」→「OK」

### 3. 使い方

- **起動**: デスクトップのアイコンをダブルクリック
- **停止**: `stop-app.bat` をダブルクリック（またはタスクマネージャーで node.exe を終了）

---

## 方法2: バッチファイルショートカット（ウィンドウ表示）

### 1. ショートカットを作成

1. デスクトップで右クリック → 「新規作成」→「ショートカット」
2. 「項目の場所を入力してください」に以下を入力：
   ```
   C:\Users\okamo\OneDrive\デスクトップ\google-calender-get-void\start-app.bat
   ```
3. 「次へ」をクリック
4. 名前を入力：`Google Calendar 空き時間`
5. 「完了」をクリック

### 2. 使い方

- **起動**: デスクトップのアイコンをダブルクリック（コマンドウィンドウが開く）
- **停止**: コマンドウィンドウを閉じる

---

## 停止用ショートカットも作成（推奨）

方法1を使う場合、停止用のショートカットも作っておくと便利です：

1. デスクトップで右クリック → 「新規作成」→「ショートカット」
2. 「項目の場所を入力してください」に以下を入力：
   ```
   C:\Users\okamo\OneDrive\デスクトップ\google-calender-get-void\stop-app.bat
   ```
3. 「次へ」をクリック
4. 名前を入力：`Google Calendar 停止`
5. 「完了」をクリック

---

## トラブルシューティング

### ダブルクリックしても何も起きない

1. ショートカットを右クリック → 「プロパティ」
2. 「リンク先」が正しいか確認
3. フルパスが正しいか確認

### ブラウザは開くがページが表示されない

- サーバーの起動に時間がかかっている可能性があります
- 30秒待ってから手動でリロード（F5）してください

### ポートが使用中エラー

- 既にサーバーが起動している可能性があります
- `stop-app.bat` を実行してから、もう一度起動してください

### node.exe が見つからない

- Node.js がインストールされていない可能性があります
- https://nodejs.org/ からインストールしてください

---

## スタートアップに登録（自動起動）

PC起動時に自動で起動したい場合：

1. `Win + R` キーを押す
2. `shell:startup` と入力してEnter
3. スタートアップフォルダが開く
4. 上で作成したショートカットをコピー＆ペースト

---

## その他の便利な使い方

### タスクバーにピン留め

1. ショートカットを右クリック
2. 「タスクバーにピン留めする」をクリック

### キーボードショートカットを設定

1. ショートカットを右クリック → 「プロパティ」
2. 「ショートカットキー」欄をクリック
3. 好きなキーを押す（例：`Ctrl + Alt + C`）
4. 「適用」→「OK」
