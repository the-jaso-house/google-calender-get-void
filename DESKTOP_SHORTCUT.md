# デスクトップショートカットの作り方

このガイドでは、デスクトップアイコンをダブルクリックするだけでアプリを起動できるようにする方法を説明します。

## 2つの起動方法

### 方法1: シンプル起動（おすすめ）

**特徴：**
- ✅ 完全にバックグラウンドで動作（ウィンドウなし）
- ✅ 8秒で起動（高速）
- ✅ ブラウザだけが開く
- ❌ 停止は `stop-app.bat` をダブルクリック

### 方法2: タスクトレイ版

**特徴：**
- ✅ タスクトレイにアイコン表示
- ✅ 右クリックでいつでも停止可能
- ✅ ブラウザを再度開くことも可能
- ❌ 少しだけ複雑

---

## 方法1: シンプル起動（おすすめ）

### ショートカット作成手順

#### 起動用ショートカット

1. **デスクトップで右クリック** → 「新規作成」→「ショートカット」

2. **場所を入力：**
   ```
   C:\Users\okamo\OneDrive\デスクトップ\google-calender-get-void\start-app-silent.vbs
   ```

3. **名前を入力：**
   ```
   Google Calendar
   ```

4. **完了**をクリック

#### 停止用ショートカット

1. **デスクトップで右クリック** → 「新規作成」→「ショートカット」

2. **場所を入力：**
   ```
   C:\Users\okamo\OneDrive\デスクトップ\google-calender-get-void\stop-app.bat
   ```

3. **名前を入力：**
   ```
   Google Calendar 停止
   ```

4. **完了**をクリック

### 使い方

**起動：**
1. 「Google Calendar」アイコンをダブルクリック
2. 何も表示されない（正常です）
3. 8秒待つ
4. ブラウザが自動で開く

**停止：**
1. 「Google Calendar 停止」をダブルクリック
2. 一瞬で終了（ウィンドウも出ない）

---

## 方法2: タスクトレイ版

### ショートカット作成手順

1. **デスクトップで右クリック** → 「新規作成」→「ショートカット」

2. **場所を入力：**
   ```
   C:\Users\okamo\OneDrive\デスクトップ\google-calender-get-void\start-with-tray.vbs
   ```

3. **名前を入力：**
   ```
   Google Calendar (トレイ版)
   ```

4. **完了**をクリック

### 使い方

**起動：**
1. 「Google Calendar (トレイ版)」をダブルクリック
2. タスクトレイ（画面右下）にアイコンが表示される
3. 通知が表示：「サーバーが起動しました」
4. ブラウザが自動で開く

**停止：**
1. タスクトレイのアイコンを右クリック
2. 「サーバーを停止して終了」をクリック

**ブラウザを再度開く：**
1. タスクトレイのアイコンを右クリック
2. 「ブラウザを開く」をクリック

---

## アイコンの変更（オプション）

### 1. プロパティを開く

ショートカットを右クリック → **「プロパティ」**

### 2. アイコンの変更

- **「アイコンの変更」** ボタンをクリック
- 警告が出たら **「OK」**

### 3. アイコンを選択

カレンダーっぽいアイコンを選んで **「OK」** → **「適用」** → **「OK」**

---

## さらに便利に

### タスクバーにピン留め

1. ショートカットを右クリック
2. 「その他」→「タスクバーにピン留めする」

→ タスクバーから1クリックで起動！

### キーボードショートカット

1. ショートカットを右クリック → 「プロパティ」
2. 「ショートカットキー」欄をクリック
3. 好きなキーを押す（例：`Ctrl + Alt + C`）
4. 「適用」→「OK」

→ キーボードだけで起動！

### PC起動時に自動起動

1. `Win + R` → `shell:startup` と入力
2. スタートアップフォルダが開く
3. ショートカットをコピー＆ペースト

→ PC起動で自動起動！

---

## トラブルシューティング

### Q: ダブルクリックしても何も起きない

**確認1: ファイルが存在するか**

エクスプローラーで以下を開く：
```
C:\Users\okamo\OneDrive\デスクトップ\google-calender-get-void
```

以下のファイルがあるか確認：
- `start-app-silent.vbs` ✓
- `stop-app.bat` ✓
- `start-with-tray.vbs` ✓（タスクトレイ版）

**確認2: git pullしたか**

PowerShellで：
```powershell
cd C:\Users\okamo\OneDrive\デスクトップ\google-calender-get-void
git pull origin claude/google-calendar-free-time-ZCBDZ
```

---

### Q: 「ポートが使用中」エラー

**原因：** 既にサーバーが起動している

**対処：**
- 「Google Calendar 停止」をダブルクリック
- または、タスクマネージャーで `node.exe` を終了

---

### Q: ブラウザは開くが、ページが表示されない

**原因：** サーバーの起動に時間がかかっている

**対処：**
- 10秒待つ
- ブラウザで `F5` を押してリロード

---

### Q: タスクトレイ版でPowerShellエラー

**解決：**

PowerShellを管理者として実行：
```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

「Y」を押してEnter

---

## 比較表

| 項目 | シンプル版 | タスクトレイ版 |
|------|-----------|---------------|
| ウィンドウ表示 | なし | なし |
| 起動速度 | 8秒 | 8秒 |
| 停止方法 | ショートカット | 右クリックメニュー |
| 再度ブラウザを開く | 手動で開く | 右クリックで開ける |
| 複雑さ | ★☆☆ | ★★☆ |

---

## おすすめの構成

### デスクトップに配置

```
🗓️ Google Calendar         （起動用）
🛑 Google Calendar 停止     （停止用）
```

### タスクバーにピン留め

```
[🗓️]  Google Calendar
```

これで完璧です！
